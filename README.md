# docker-add-vs-copy-demo-

## Local dev

This repo contains static HTML files under `App-Files/`. Run a simple local server:

```bash
npm run dev
```

Then open:

- http://localhost:8000/index.html

If you need a different port, edit the `dev` script in `package.json`.